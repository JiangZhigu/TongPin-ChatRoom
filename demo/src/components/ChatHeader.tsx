import { Avatar } from '../AttachmentViews';
import { ArrowLeft, ClipboardList, MessageCircle, PanelRight, Plus, Search } from 'lucide-react';

export function ChatHeader({ avatarUrl, title, description, onBack, onToggleDetails, detailsOpen = false, group = false, onSearch, onOpenTasks, onCreateTask }: { avatarUrl?: string | null; title: string; description?: string; onBack: () => void; onToggleDetails?: () => void; detailsOpen?: boolean; group?: boolean; onSearch?: () => void; onOpenTasks?: () => void; onCreateTask?: () => void }) {
  return <><header className="chat-header"><button className="icon-button back-button" onClick={onBack} aria-label="返回会话列表"><ArrowLeft size={21} /></button>{group && !avatarUrl ? <span className="avatar group-avatar" aria-hidden="true"><i /><i /><i /><i /></span> : <Avatar url={avatarUrl} label={title} />}<div className="chat-heading"><h1>{title}</h1>{description && <p>{description}</p>}</div><div className="chat-header-actions">{onOpenTasks && <button className="text-button chat-task-entry" onClick={onOpenTasks}><ClipboardList size={15} />群待办</button>}{onCreateTask && <button className="icon-button chat-create-task" aria-label="新建待办" title="新建待办" onClick={onCreateTask}><Plus size={18} /></button>}{onSearch && <button className="icon-button" aria-label="搜索 / 收藏" title="搜索 / 收藏" onClick={onSearch}><Search size={19} /></button>}{onToggleDetails && <button className="icon-button" onClick={onToggleDetails} aria-label={group ? '群详情与管理' : '会话设置'} title={group ? '群详情与管理' : '会话设置'} aria-expanded={detailsOpen}><PanelRight size={20} /></button>}</div></header>{group && <nav className="chat-tabbar" aria-label="群会话内容"><span className="chat-tab active" aria-current="page"><MessageCircle size={15} />聊天</span>{onOpenTasks && <button className="chat-tab" onClick={onOpenTasks}><ClipboardList size={15} />待办</button>}</nav>}</>;
}
