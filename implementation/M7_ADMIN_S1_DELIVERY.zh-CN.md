# M7-Admin 第一批交付记录

本批为完整SA01–SA14中的SA01–SA06：概览、运行监控、用户、设备连接、好友关系及群治理。共用处置包含准确目标预览、操作理由、密码与第二因素再认证、固定操作ID、事务内权限/版本复核、单目标执行及每批最多10项的持久批量任务。内容、文件、举报、配置、公告、安全、审计和运维页面继续在后两批完成。接口见[S1契约](M7_ADMIN_S1_CONTRACTS.zh-CN.md)。

本批后台13项检查最终通过，42.69秒，覆盖当前授权、并发幂等、预览冲突、最后可用超管、真实人工改密恢复、群邀请与成员期、部分成功、中途降权、监控和告警。受影响的账号/聊天/群/文件/注销59项检查通过，200.11秒；基础设施8项通过，5.35秒。新增任务异常测试先复现任务失败而命令仍执行中的问题，再验证任务与未完成目标在同一事务收尾；监控采集故障不再阻断会话检查。

界面首批38项通过，随后分别增加账号限制刷新7项、凭据期限/中文状态/关系搜索3项、监控刷新生命周期7项，均通过各自直接检查。它们是连续版本的定向证据，不把各次完整测试文件重复相加。监控刷新先复现旧告警不更新，再补齐手动联动、可见页面10秒轮询、慢请求不重叠、隐藏暂停、身份取消、错误旧数据标记及表格节点保留。最终TypeScript与Vite构建通过：`index-BnDNKOkG.js` 540.80kB/gzip159.08kB；独立表情包1723.26kB/gzip186.91kB。主包/表情包超过500kB和outbox重复静态/动态导入提示仍如实保留。

第9轮真实Chrome使用项目内独立DATA_DIR、两个随机超管及三个普通账号，前置会话撤销后从真实验证码页面登录。以下是实际页面与服务观察：

- 六页均返回真实数据；会话页面分别显示一个有效设备和一个对应实时连接，群详情显示两个成员、一个已使用邀请名额和一个待审预留。
- 管理员实际提交上传/建群限制后，普通账号无需刷新收到新限制和原因，正在编辑的简介完整保留。建群请求403；图片实际进入待发后上传预留403，显示发送失败及限制理由，数据库无附件记录且未新增群。
- 全站禁言使聊天输入不可用并显示理由；解除后实际发送一条消息，页面从待确认变为已发送，数据库仅一条、WebSocket实际请求计数1且错误0。
- 撤销群邀请后预留名额从1变0，关联待审申请到期。封禁后普通页面自动返回登录，当前实时连接数从1变0；再次输入正确密码和验证码后显示本人封禁理由。
- 隔离入队一个未知类型持久任务，真实后台执行器将其标记失败，实际监控生成告警。恢复该隔离任务状态后，最终界面自动显示已恢复；原告警表格节点保持，视口位置仅变化约0.9px，浏览器滚动锚定随上方采样行增长调整滚动量。
- 320/390/768/1024/1440/1920窗口宽度无整页横向溢出；320宽的管理导航可展开，预览对话框位于视口内。最终版本关系页恢复URL搜索条件并显示“已接受”。这不是物理手机/软键盘或三平台证据。

禁言截止值通过原生日期输入的DOM赋值及input/change事件填入，因为浏览器工具的普通fill未改变该复合输入；提交、预览、再认证及数据库结果仍为真实流程，不宣称操作了系统日期弹窗。重启前主要动作使用`index-BaWn6lZU.js`，同一轮保留数据库重启后，监控修复与最终关系筛选使用`index-BnDNKOkG.js`。进程指标在重启后明确重新计数。

两个浏览器的console消息和page error均为空。脱敏网络记录：管理端118次200及一次预期目标状态冲突409；普通端725次200、三次预期403和封禁后一次401。记录仅保留方法/路径/状态/类型，不保留请求头、Cookie或查询参数。数据库完整性ok、外键违规0。两个浏览器及隔离服务已关闭，8778端口可重新绑定。源码与原始报告由项目验证索引关联，待本批冻结审查完成后继续SA07–SA14。

## 独立审查后的定向修复

冻结1a2aac0648e7c1af14620708e3ca0e70e0adf868的审查发现两项P2：管理会话外键会阻止超管到期注销；同群多邀请批量撤销的群级版本相互影响。现已定向修复：到期注销删除未提交预览、清空旧会话凭据摘要及设备资料，仅为已提交命令保留不可认证的最小会话引用；邀请撤销使用自身邀请状态与群访问/群主状态指纹，其他邀请的同批正常变更不再造成假冲突。

定向复现先得到3个真实行为失败（两类管理历史的注销外键失败、同群双邀请只成功首项），另有1个测试输入类型错误已纠正。修复后的4个新增交叉场景与既有8个生命周期用例共12项通过，59.66秒；旧凭据拒绝、普通无管理记录账号物理清理、命令审计关系保留和真实外部修改冲突均有断言。测试使用S1既有1–7迁移，S2未提交迁移暂存于本地准备目录，未作为此修复的前提。前端未改，无新增浏览器轮次；等待该修复提交的独立复核。
