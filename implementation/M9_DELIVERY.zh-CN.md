# M9：可部署交付准备

更新：2026-09-12。状态：本页保存M9阶段的脚本、原冻结全量、真实升级/回滚演练和原三平台CI证据。注册/登录追加结果见[追加记录](REGISTRATION_LOGIN_DELIVERY.zh-CN.md)；后续UI整合、真实体验缺陷修复、最新CI及自动安装器见[UX记录](UX_REVIEW.zh-CN.md)与[实施状态](STATUS.md)，不与下述历史数值混用。最终同等级Astra正在完成剩余真实Edge页面体验，最终包在其结束后冻结。未进行生产上线。

## 已实现的入口

- Windows的 `tongpin.cmd` / `tongpin.ps1` 与macOS/Linux的 `tongpin.sh` 共用 `scripts/deploy.py`，从自身目录定位工程，优先本版本 `.venv`，再实际探测可用解释器。
- `doctor` 只读检查系统/发行版、运行时与包管理器；Python层的 `deploy.py install` 安装本项目锁定环境，明确是否允许项目内Python下载。后续新增的原生 `install.cmd` / `install.sh` 在缺Python时可先通过系统包管理器安装引导解释器，Linux/macOS按正常权限提示认证；详见[自动安装说明](../INSTALL-PYTHON.zh-CN.md)。系统服务模板仍不会自动安装或启动。
- `precheck` 使用候选版本自己的解释器，只读核对生产配置、HTTPS来源、外部密钥、数据库/迁移、首位管理员及第二因素、运营信息/条款、磁盘和文件策略；缺项返回非零，生产启动会重做预检。
- `activate` / `rollback` 使用控制锁、数据实例锁、候选文件/解释器验证、升级前逐文件快照、迁移与SQLite完整性检查，最后原子切换版本指针。版本、数据、控制和快照目录分离。回滚只切换兼容代码，不倒退当前消息、权限或凭据消费。
- `release.py` 检查前端构建回执与已提交交付文件，创建带逐文件清单的ZIP及SHA256；暂存先完整验证，再写全新目录，拒绝路径穿越、链接、大小写冲突、Windows保留路径与超限包。激活时重核对解压后的清单。
- Docker分阶段构建，运行用户10001、单实例、持久挂载、健康检查；Compose使用只读根目录、有限tmpfs、移除capabilities、主机回环端口。提供Nginx、systemd、launchd及Windows前台托管模板。

具体操作见 [安装](../docs/INSTALL.zh-CN.md)、[配置](../docs/CONFIGURATION.zh-CN.md)、[用户指南](../docs/USER_GUIDE.zh-CN.md)、[管理员指南](../docs/ADMIN_GUIDE.zh-CN.md)、[运维与升级](../docs/OPERATIONS.zh-CN.md)。本地相对文档链接已经检查。

## 本机已执行

部署专属21项测试全部通过，包括归档哈希/条目检查、拒绝覆盖、解压后文件变更拒绝、前端回执来源/产物变更、快照字节及隔离、实例锁与兼容schema指针回滚、真实离线运营配置审计、从外部工作目录执行CMD入口、生产预检失败/通过前后数据库哈希不变。指针用例明确隔离了依赖探测，其结论不代替下面的真实发布演练。

交付审查发现M9-R01：继承 `UV_PROJECT_ENVIRONMENT` 等设置可能使安装修改其他项目。修复显式固定项目、工作目录、虚拟环境、缓存和解释器下载目录，启动uv前拒绝路径中的symlink/junction，保留代理和证书设置。新增7项包括参数/环境核对、5处真实Windows junction拒绝，以及真实uv离线同步；外部隔离项目全部原有文件逐字节不变。记录 `M9-INSTALL-ISOLATION-01-01`；两文件提交 `ada1feca9821b34276061c7cd981c7faecb6c111` 经 `M9-INSTALL-REVIEW-01-01` 增量复审，P1关闭，无新增可操作发现。

真实服务检查：正式 `python -m tongpin` 入口启动，WebSocket101、提交后ACK、其他成员HTTP读取、恰好一条持久消息、完整性/外键及进程停止均通过。不是Flask内存适配器检查。

Windows真实浏览器检查 `M9-CI-BROWSER-01-03`：Chromium153.0.8010.12，两个隔离context；真实Socket.IO连接、消息未刷新接收/刷新后保留；通过界面创建个人待办并刷新后读取。独立数据库确认消息和待办各一条。聊天/待办320、768、1440六个视口无横向溢出，console/pageerror/意外网络/5xx为0。测试服务停止，临时会话文件删除。初次缺少锁定浏览器及之后脚本等待不足的失败报告保留，修复的是检查脚本。

该浏览器脚本属于CI冒烟检查，不抵扣用户指定的最终新Astra完整体验；工具按2026-09-12追加授权使用agent-browser驱动Edge。真实软键盘、物理输入法、目标主机TLS/防火墙/独立故障域备份仍须分别记录。

## 三平台CI与发布状态

[CI配置](../.github/workflows/ci.yml) 使用公开仓库标准 `ubuntu-latest`、`windows-latest`、`macos-latest`；锁定Python3.12.13、Node24.15.0、uv0.11.27，构建后检查真实HTTP/WS、SQLite、缓存、文件、恢复、部署脚本和浏览器。另一个标准Ubuntu任务构建Docker镜像，并在只读根目录/持久挂载下运行镜像内HTTP和WS检查。仅Markdown文档变化不重复触发，代码/脚本/锁文件/配置/工作流变化仍触发，手动执行入口保留。

首轮 [34635448383](https://github.com/JiangZhigu/TongPin-ChatRoom/actions/runs/34635448383) 的Ubuntu41项后端、435项前端、真实浏览器与Docker任务通过；Windows/macOS在setup-python安装阶段失败，尚未执行应用检查。官方Actions清单只有Linux的3.12.13构建；修正为Linux直接使用3.12.13、Windows/macOS用3.12.10引导，再由安装器在项目内取得锁定3.12.13，应用运行版本未降级。

第二轮 [34635910818](https://github.com/JiangZhigu/TongPin-ChatRoom/actions/runs/34635910818) 三平台安装、构建、静态检查与41项后端均通过；Windows435项前端和真实浏览器、Docker任务通过。Ubuntu/macOS各有一个App账号失效fixture测试失败，均止于浏览器之前。随后只改App测试替身生命周期与受控失效请求时点，最终本机App18/18及类型检查通过，再执行第三轮远端完整验证。两轮失败报告及日志完整保留。

最终第三轮 [34637137650](https://github.com/JiangZhigu/TongPin-ChatRoom/actions/runs/34637137650) 在提交 `e3c066fa9b3617dfca9734e5f8f567e0463c0260` 上**四个任务全部通过**。三平台各有41项后端平台检查、435项前端测试；浏览器从正式服务创建/接收消息、刷新后读取消息与个人待办，独立库核对各一条，完整性ok，进程已停止。记录 `M9-THREE-PLATFORM-CI-01-03`。

| 实际环境 | 应用Python / SQLite | 本轮实测结果 |
|---|---|---|
| Windows Server 2025，10.0.26100，x64 | 3.12.13 / 3.53.1 | 41项后端25.22秒、435项前端、真实浏览器全部通过 |
| Ubuntu 24.04.5，x64 | 3.12.13 / 3.45.1 | 41项后端16.27秒、435项前端、真实浏览器全部通过 |
| macOS 26.6.2，arm64 | 3.12.13 / 3.53.1 | 41项后端18.27秒、435项前端、真实浏览器全部通过 |
| Docker Debian 12，Linux x64 | 3.12.13 / 3.40.1 | 镜像构建、doctor、版本、非root/只读根/持久挂载下WS101、提交后ACK、接收者HTTP与持久消息通过 |

41项是各平台直接相关后端范围；本机212项后端全量和M8性能证据另记，不把435项乘以平台数当作不同功能数量。日志保存在项目本地，以上公开CI链接可直接查看。CI浏览器冒烟不能替代最终独立UX-POST。

2026-09-12再次核对仓库为public、具有push权限。官方文档确认公开仓库标准运行器免费；larger runner始终收费。本工作流不使用larger runner、缓存上传、产物上传、Packages或其他计费资源。[GitHub Actions计费说明](https://docs.github.com/en/billing/concepts/product-billing/github-actions)。代码已由主智能体按FE06授权快进推送至上述实测提交；之后仅交付Markdown文档更新，不重复运行未受影响的CI。

候选包 `tongpin-2658025-candidate.zip` 已生成：355文件、1,951,774字节，SHA256 `c1db71c423ed736388966f68eb4cd96e1a1c9722a2884c38ff69fc4f99030bcc`。从校验后的包暂存A/B两个独立目录，真实安装各自 `.venv`，确认导入和进程来自各候选目录。通过正式控制入口启动A、写入消息/个人待办/附件、拒绝运行中切换；停止后激活B，读取旧数据并新增内容；停止后回滚A，2条消息、2项待办及附件原字节保留。3份升级前快照逐文件校验，SQLite完整性ok、外键违反0，三个服务进程均停止。记录 `M9-RELEASE-DRILL-01-01`。

演练A/B使用同一提交和相同schema，验证版本控制与数据保留机制，不冒充历史版本迁移验收。第一次观察脚本误读待办响应结构的失败报告保留；更正观察路径后完成演练。该历史候选安装时为保护fixture显式清除了继承的uv重定向；之后已用下述修复版候选另做真实安装。生产文件策略下该附件如实隔离，不声称已扫描通过。

安装修复后的 `6154ce0` 候选另做真实完整依赖安装，记录 `M9-PACKAGED-INSTALL-01-01`：355文件、1,954,863字节，SHA256 `2758a60d01341eb16c1bda44af333f53d07a65cc5820d33b72f830edbd573a36`。保留指向外部隔离项目的4个UV重定向设置执行安装，依赖仍装入候选自己的 `.venv`，受保护外部文件哈希不变；核对实际导入路径和解释器prefix后，以包内代码验证WS101、提交后ACK、另一账号读取、唯一持久消息和SQLite完整性。服务停止后355份包内清单仍全部匹配。在后来的注册追加之前，M9收尾只变更CI/测试fixture及文档，当时已通过字节一致性映射复用这份安装证据。注册追加改变了认证应用代码，其定向检查、候选源码启动和新CI结果另记，不沿用旧应用代码完全一致的结论。

历史M9本地包定位为 **UX前候选**，路径 `dist/releases/tongpin-0.1.0-pre-ux.zip`，随附 `.sha256` 与外部交付回执。回执记录当时文档提交、包字节数、355份清单及与6154ce0真实安装候选的逐文件比较，避免把包自身校验值写进包内形成循环。打包器拒绝未提交交付文件和过期构建回执；主智能体在包生成后核对清单、实际暂存及132个构建输入/5个输出。当时允许变化仅限CI、App测试fixture、构建回执和五份交付Markdown；应用运行代码、依赖锁及前端产物均完成字节一致性映射。

注册追加后的最新体验候选是 `dist/releases/tongpin-0.1.0-edge-ux-candidate.zip`，对应53b0afe，357文件、1,970,406字节，SHA256 `9b0478adfe467a434c2410ab073d59fa065dfa1996f701670b3155e089209052`。新目录解包的清单、132个构建输入及5个输出已核对；最终Astra从该候选和独立数据执行页面体验。现阶段它仍是体验候选，最终交付包及结论待体验和必要修复完成后更新。

最终Astra体验、范围内缺陷修复与受影响复测仍须完成后更新候选和正式交付结论。生产域名、主机、TLS证书和真实运营密钥未配置，M9实际上线不在默认授权内。
