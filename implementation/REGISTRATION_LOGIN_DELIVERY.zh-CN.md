# 开放注册与按需登录验证码

更新：2026-09-12。状态：实现、本机直接相关检查及独立审查通过。首轮新三平台CI的后端全部通过；前端定位到一个旧群邀请测试仍要求登录验证码，现已修正且该文件2项检查及类型检查通过，等待新CI。之后进行最终独立Edge完整体验。

## 最终行为

- 新站默认开放普通用户自主注册；后台保留开放、仅邀请码、关闭三态，已有显式保存策略继续生效。`/register`可直接进入注册表单。
- 注册必须填写真实图形验证码；账号成功创建后展示8个恢复码，确认保存后进入聊天室。密码保存为Argon2散列，恢复码只保存散列。
- 正常登录只要求用户名和密码，管理员继续要求第二因素。前4次密码错误不请求或显示验证码，第5次返回明确提示并展示验证码；之后先通过验证码才能再次验证密码。
- 服务端按来源IP与不区分大小写的用户名记录失败；刷新页面或重启服务不能清除有效记录。完整登录成功清零；15分钟没有新的密码错误后失效。错误验证码、第二因素错误不计入密码失败。
- 持久计数复用有容量上限的数据库表，固定数量锁串行处理同一计数键，避免并发越过阈值。注册、找回账号的验证码与既有登录速率限制继续生效。

## 本机验证

- 首轮选定68项认证、管理员及部署用例：65通过，3项新增部署测试因测试库父目录未创建而失败。修复测试准备后，11项新认证用例与3项部署策略用例全部通过（14项，83.37秒）。合并不同用例后68项均有通过证据，未把重复的11项累计为新功能数。
- 新认证用例覆盖阈值、挑战前密码验证拦截、成功重置、用户名大小写与来源隔离、15分钟过期、真实Runtime停止重建后的计数、并发密码尝试、管理员第二因素、注册/找回强制验证码及计数容量边界。
- 界面2个直接相关测试文件35项通过，TypeScript与全项目Python静态检查通过。初轮一个旧StrictMode断言等待正常登录验证码，按新契约调整后通过。
- 使用正式入口、独立DATA_DIR和真实Chrome页面创建普通账号：注册201、8个恢复码确认、已连接聊天室、无验证码密码登录、前4次错误、第5次验证码、刷新后的服务器挑战、正确验证码登录、重置后再错一次仍无验证码及密码登录全部通过。
- 捕获188个网络请求；认证序列与页面步骤一致，无HTTP 5xx、控制台消息或未捕获页面错误。只读检查确认1个真实注册账号、8个恢复码、Argon2密码、成功后无失败计数，数据库完整性为ok。247个源码/构建文件哈希未变；本轮浏览器和隔离服务已关闭。

## 交付边界与后续

本批尚不能将旧M8的212项后端/435项前端全量、旧M9的三平台41项后端/435项前端称作新版本全量。[首轮新CI](https://github.com/JiangZhigu/TongPin-ChatRoom/actions/runs/34671447637)对应合并提交`54e311ba7091154f7a77b14c5b8993d857f9aedf`：Windows、Ubuntu、macOS各63项后端通过，前端各444通过、1失败；三个浏览器步骤均未启动。失败为旧群邀请登录继续测试寻找正常登录时已取消的验证码按钮。已保留StrictMode片段清除、跨登录邀请继续及不自动申请断言，新增无验证码请求及精确登录请求体检查；该文件2项检查（2.34秒）和类型检查通过。Docker真实HTTP/WS通过。原失败日志保留，后续CI另记，不把失败批次写成通过。

认证实现提交`5f1d3c12833513f9edf2d0a2eda7c68271a2af66`及上游Windows Unicode/emoji固定LF提交`19f27714a172227f5a6714b1ca48589bb2b9c817`均经过只读审查，无可操作发现。合并保留双方历史；测试记录的混合换行与冻结内容已完成252条字节归一化映射，不构成业务差异。

新候选`dist/releases/tongpin-0.1.0-registration-pre-ux.zip`包含357文件、1,968,388字节，SHA256 `1325d36a1195ea8d5708c15e7e16838ba0fa6c8fbaf6f2ef3311d98a4bdb0f54`。解包后完整清单、132构建输入/5输出匹配；显式从解包源码运行正式入口，真实WS101、提交后ACK、接收者HTTP读取、1条持久消息及SQLite完整性ok，进程正常停止。该演练使用既有锁定解释器，不称为重新安装依赖。原017332c候选保留，最终体验后再刷新交付包。

用户已明确允许最终同思考等级新Astra使用computer-use或agent-browser（Edge）；本任务采用后者，完整体验聊天室、待办、账号设置与SA01–SA14后台，并修复、复测发现的问题。上述本机定向检查不替代该独立步骤。所有账号及数据均为项目内隔离测试数据；未执行实际生产部署。
