# M0 风险、暂定规则与确认点

日期：2026-09-11；任务 tongpin-m0-20260911-01a08d87。当前用户指令优先于原设计包。后端 Python + Flask 已确定；不再考虑 Vercel。这两项无需再次询问。

## 用户已经确定

1. 后端 Python + Flask；SQLite；Windows/macOS/Linux 可运行的进程内缓存；常驻服务和持久磁盘。
2. 账号、好友私聊、自建群、邀请链接、群主/管理员/群员、上线提醒、两类离线、图片/文件、emoji 全部必交。
3. 沿用同频浅灰浅蓝聊天形态，保留 IME 和滚动体验；正式入口不保留模拟登录、验证码答案、假上传、假 ACK、固定用户/人数。
4. 用户已把执行方式明确为“全部开发事项一次确认后，交由主智能体连续执行”；[FE-1 总单](FULL_EXECUTION_BASELINE.zh-CN.md)已经用户整体确认，当前连续实施。不付费、不配置生产密钥、不实际部署、不抹正式数据库的边界继续保留。
5. 产品验收后另起一个同思考等级 Astra 子智能体，用 browser-use 对聊天室和后台完整做用户体验测试；该步骤未执行。
6. 用户已自行创建 [JiangZhigu/TongPin-ChatRoom](https://github.com/JiangZhigu/TongPin-ChatRoom)。只读确认 Public、main、1 个初始化提交和 Apache-2.0 LICENSE；先前私有空仓库建议已被实际状态取代。M1已建立Git关联并保留初始化历史。
7. 后台为监控和管理整个 Chatroom 的超级管理员后台。超管可审阅全站私聊、群聊及附件，敏感访问留审计记录。这两项用户已明确，不再作为“是否只做群管理/缩小后台”的待选项。

## 决策状态：C1/C2/C3及连续执行均已确认

| ID | 推荐决定 | 为什么影响下一阶段 | 当前状态 |
|---|---|---|---|
| C1 工程与运行方案 | 当前项目根：Python 3.12.13 项目 .venv；Flask HTTP + python-socketio ASGI + Uvicorn 单进程；前端 React/TypeScript/Vite；Python uv.lock 与前端 package-lock.json 各一个锁 | 后端语言已由用户确定；这里确认目录、前端和跨平台常驻运行组合，随后 M1 才创建工程 | 已随FE-1确认 |
| C2 产品基线 | 接受下表暂定规则作为首期可配置默认值 | 身份、恢复、好友、群历史和邀请规则影响后续数据库与鉴权；一次性确认以免反复变更 | 已随FE-1确认 |
| C3 后台范围 | 全站超级管理员后台；SA01–SA14覆盖监控、账号/会话/关系/群组/内容/附件、配置、公告、安全、审计和运维；私聊/群聊/附件均可审阅并审计 | 用户已明确扩展为全站管理产品；权限模型、索引、数据保留、管理页面与验收需相应增加 | 定位及内容审阅范围已确认 |

C3 详细范围、数据/权限边界、阶段安排与 SA01–SA14 验收映射见 [全站超级管理员后台](M0_SUPER_ADMIN_SCOPE.zh-CN.md)。站点超管权限独立于群角色；管理接口可在明确授权后覆盖全站，不依赖是否为好友或群成员。私聊、群聊与附件的敏感读取须审计；管理动作再认证、记录原因并显示真实结果。规划中的受控导出和批量操作有范围/数量限制，防止一次误操作波及全站。

所有待定项汇入 [FE-1 全流程总单](FULL_EXECUTION_BASELINE.zh-CN.md)，包括下表 C2 和新增的超管安全、人工恢复、保留/注销、导出、公开推送/CI、最终交付范围。表中的阶段表示实现依赖，不再表示每阶段向用户提问；整体确认后自动执行。默认交付可部署版本，实际生产上线在授权范围外；如果用户本次指定上线环境和边界，则按本次授权实施。

## 业务基线：已随FE-1整体确认

| 规则 | 暂定值 / 行为 | 实现依赖阶段（总单中一次确认） |
|---|---|---|
| 登录名 | ASCII 字母开头，4–24 位字母/数字/下划线，小写唯一；昵称 1–32 Unicode 码点，可重复 | M2 |
| 密码 | 15–128 码点，不 trim/截断；弱密码拒绝；Argon2id、独立盐、参数可升级 | M2 |
| 注册验证码 | 图形 CAPTCHA；120 秒、最多 5 次、一次消费、绑定匿名流程；不发短信/邮件 | M2 |
| 账号恢复 | 8 组至少 128 位熵的一次性恢复码，仅展示一次/仅存摘要；无邮箱且全部丢失时无自动恢复后门 | M2 |
| 会话 | 常规 idle 24 小时/absolute 7 天；保持登录 idle 7 天/absolute 30 天；敏感再认证 5 分钟、action-bound、一次性 | M2 |
| 注册准入 | 本地测试可开放；公开部署默认 closed/invite-only；站点邀请码与群邀请码不同 | M2/M9 |
| 私聊 | 双向好友后允许新增；删好友后双方旧历史可读；任一方屏蔽阻止新增与上线提醒 | M3 |
| 群规模 | 每群 200 人；每人最多 20 个活跃自建群 | M5 |
| 邀请 | 默认群主/管理员创建；审核制；24 小时/10 个名额；pending 原子预留，拒绝/撤销/过期释放，批准再次核验 | M5 |
| 群历史 | since_join；只看当前加入期后的消息；退出立即失权，再入不恢复旧期；HTTP/WS/搜索/引用/附件一致 | M5 |
| 转让群主 | 再认证、受让成员接受、24 小时过期；成功后原群主降普通成员 | M5 |
| 消息长度 | 4,000 码点且 UTF-8 ≤16 KiB；空白拒绝、纯文本；消息 sequence 以十进制字符串传输 | M3 |
| 本机离线队列 | 文本 ≤100 条/7 天；Blob 合计 ≤50 MiB；保存失败可见，账号隔离，多标签页唯一投递者 | M4/M6 |
| 图片/文件 | 图片 ≤10 MiB、文件 ≤25 MiB；单条 ≤6 件/合计 ≤50 MiB；单用户配额初值 1 GiB，磁盘高水位初值 90% | M6 |
| 文件种类 | 图片 PNG/JPEG/WebP/GIF；PDF/TXT/MD/CSV/JSON/Office/ZIP/7Z 按白名单；一般文档强制下载、不自动解压 | M6 |
| 上线/通知 | 好友上线提醒逐好友默认关闭；受隐私/屏蔽约束；浏览器通知主动授权；关浏览器不保证继续发送或 Web Push | M4/M7 |
| 已读/撤回 | 私聊可关已读；群只维护本人阅读；本人 2 分钟撤回，管理删帖单独审计并保留墓碑 | M7 |
| 缓存/presence | 查询 10k 项/64 MiB/30–60秒 TTL；验证码独立 10k/120秒；在线连接集合独立且有上限 | M1/M4 |
| 游标/备份 | user_events 30 天；备份初值 7 日+4 周，生产独立故障域 | M4/M8/M9 |
| 首期不交付 | E2EE、音视频、原生手机 App、公开群目录、付费功能、浏览器关闭 Web Push 保证 | 本次确认范围 |

首期正式 emoji 使用固定 Unicode + CLDR 简中数据，保留英文 fallback 与许可证；原型 2,810 个序列只是迁移基线，不确认其全量覆盖。版本在 M7 核查后锁定。

## 风险与退出条件

| ID | 风险 / 事实 | 计划处理与阶段门槛 |
|---|---|---|
| R01 原型被误作产品 | 所有认证/上传/ACK 当前都是本地模拟 | M1 生产入口空态/未启用，演示独立；M2–M7 才按真请求完成；M7 构建查残留 |
| R02 Flask 与异步传输边界 | WSGI/ASGI 适配不会自动消除同步阻塞；线程敏感适配可能串行 HTTP | M1 有界执行器、短同步事务、禁止跨 await 持事务；M8 测 HTTP+WS 混合负载 |
| R03 请求体缓冲 | WsgiToAsgi 可先 spool 请求体；只用 Flask MAX_CONTENT_LENGTH 不能证明前置资源受控 | M1 在适配前限制真实累计字节/超时/并发；M6 上传前鉴权、私有临时目录、配额和 chunked 负向测试 |
| R04 SQLite 写竞争 | WAL 仍单 writer；多个进程会拆分 presence | 每线程/操作独立连接，进程内有界写队列/锁，跨进程启动锁、WORKERS=1；第二进程与 kill/restart 测试 |
| R05 进程内缓存并发 | cachetools 不自动线程安全；TTL 不是立即清理保证 | 包装锁、条数+字节双限、注入时钟、单飞/回收；权限复核和持久限流不依赖 LRU |
| R06 跨系统 wheel/SQLite 漂移 | 当前只有 Windows；Python 所带 SQLite 随发行包变化 | 每平台记录 Python/SQLite/依赖版本；锁文件重建 .venv，测试 Argon2/Pillow 与真实 WS，缺环境标未测 |
| R07 备份与附件一致性 | 直接拷在线 db 或先删附件会产生不完整恢复 | backup API、文件清单/hash、GC 与备份联动；空目录恢复后登录/历史/附件/权限验证 |
| R08 权限撤销泄露 | 群成员失权可能绕过缓存、旧游标、引用或旧文件 URL | 同一策略服务覆盖所有读写；提交后撤订阅，广播/补拉再授权；M5/M6 负向验收 |
| R09 ACK/离线重复与串号 | 断网、掉 ACK、多标签页、换账号 | SQLite 幂等键+payload_hash；IndexedDB 持久文字/Blob；先同步授权后投递 |
| R10 文件滥用 | 白名单不等于无恶意内容 | 封闭内测限额；公网要真扫描/隔离或严格收窄；没有扫描不得假返回 clean |
| R11 全站超管扩展 | 用户明确要求全站监控/管理及私聊、群聊、附件审阅；v2原有范围和参考28表不足以覆盖 | M2起增加站点权限/审计等模型；M7-Admin独立批次交付SA01–SA14；M8及额外Astra测试均必须覆盖完整后台 |
| R12 旧规格冲突 | 旧 plan_doc 匿名/无库范围过时，用户已主动删除；v2 数据说明允许转让后保留 admin | 当前用户与 v2 PRD 优先；推荐原 owner 降 member；保留用户清理后的状态并在实施测试固定口径 |
| R13 Git/远端 | 无本地 .git；用户新建远端已有 main 初始化历史 | 初始61文件中59哈希一致、2个旧文档为用户确认删除；M1关联保留远端README/.gitignore/LICENSE和历史，不强推覆盖；当前无推送 |
| R14 验收遗漏 | 当前 ACCEPTANCE 新增 G09，原 manifest 没覆盖 | M8 交付三系统与 Docker 脚本；Linux识别发行版/环境和回退；M9交付准备自动执行，实际上线按FE08范围 |
| R15 体验工具未就绪 | Browser Use 控制调用失败，尽管扩展/本地组件检查正常 | 修复连接后再测试；M8后新Astra独立运行，缺工具列受阻，不用mock或API报告代替 |
| R16 超管高权限与敏感读取 | 管理权限可覆盖全站内容，凭据泄露或权限检查遗漏影响更广 | 独立站点授权、超管第二因素/恢复设计、再认证、最后超管保护；敏感读取及修改均审计，撤权同时关闭HTTP/WS/导出权限 |
| R17 配置与批量运维 | 配置已保存但未生效、批量动作部分失败或重试重复执行会误导管理员 | 配置版本/生效方式/回滚，固定操作ID与有界持久任务，目标数量预览，逐项结果；禁止用假进度或自杀进程代替受控运维 |

生产主机、预算、域名、实际运营信息和独立备份位置不阻塞完整开发；FE-1 默认以可部署交付为终点。注册准入、文件策略、条款配置能力与发布预检本次实现，不拖到以后再决定是否开发。只有用户要求本次实际部署时，才需在这次整体确认中补齐目标环境与授权。
