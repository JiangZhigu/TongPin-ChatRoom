import { Avatar } from '../AttachmentViews';
import { ArrowLeft, PanelRight } from 'lucide-react';

export function ChatHeader({ avatarUrl, title, description, onBack, onToggleDetails, detailsOpen = false, group = false, onSearch, onOpenTasks, onCreateTask }: { avatarUrl?: string | null; title: string; description?: string; onBack: () => void; onToggleDetails?: () => void; detailsOpen?: boolean; group?: boolean; onSearch?: () => void; onOpenTasks?: () => void; onCreateTask?: () => void }) {
  return <header className="chat-header"><button className="icon-button back-button" onClick={onBack} aria-label="返回会话列表"><ArrowLeft size={21} /></button><Avatar url={avatarUrl} label={title} /><div className="chat-heading"><h1>{title}</h1>{description && <p>{description}</p>}</div>{onOpenTasks && <button className="text-button chat-task-entry" onClick={onOpenTasks}>群待办</button>}{onCreateTask && <button className="text-button chat-task-entry" onClick={onCreateTask}>新建待办</button>}{onSearch && <button className="text-button" onClick={onSearch}>搜索 / 收藏</button>}{onToggleDetails && <button className="icon-button" onClick={onToggleDetails} aria-label={group ? '群详情与管理' : '会话设置'} aria-expanded={detailsOpen}><PanelRight size={20} /></button>}</header>;
}
